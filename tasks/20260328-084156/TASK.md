# Priority filter

- STATUS: OPEN
- PRIORITY: 50
- TAGS: feature, parser

Add a custom syntax for filtering priority levels.
This should include less, greater and equal with all its flavors.
Example:
```console
$ ./tt -f "prio > 20 and prio <= 80
```